apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uniconfig.fullname" . }}-test-connection"
  labels:
    {{- include "uniconfig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uniconfig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
