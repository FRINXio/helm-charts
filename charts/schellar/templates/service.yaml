apiVersion: v1
kind: Service
metadata:
  name: {{ include "schellar.fullname" . }}
  labels:
    {{- include "schellar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "schellar.selectorLabels" . | nindent 4 }}
