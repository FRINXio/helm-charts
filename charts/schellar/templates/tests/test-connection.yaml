apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "schellar.fullname" . }}-test-connection"
  labels:
    {{- include "schellar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "schellar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
