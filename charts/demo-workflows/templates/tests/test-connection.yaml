apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-workflows.fullname" . }}-test-connection"
  labels:
    {{- include "demo-workflows.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-workflows.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
